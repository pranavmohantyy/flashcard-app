import json\nimport os\nimport random\nimport csv\n\nclass Flashcard:\n    def __init__(self, front, back, difficulty):\n        self.front = front\n        self.back = back\n        self.difficulty = difficulty\n        self.interval = 1\n        self.last_reviewed = None\n\nclass Deck:\n    def __init__(self, name):\n        self.name = name\n        self.cards = []\n        self.stats = {\n            'total_cards': 0,\n            'due_today': 0,\n            'accuracy_rate': 0.0\n        }\n\n    def add_card(self, card):\n        self.cards.append(card)\n        self.stats['total_cards'] += 1\n\n    def review_card(self, card, rating):\n        if rating > 3:\n            card.interval *= 2\n        elif rating < 3:\n            card.interval = max(1, card.interval // 2)\n        card.last_reviewed = self.get_today()\n\n    def get_due_cards(self):\n        return [card for card in self.cards if self.is_due(card)]\n\n    def is_due(self, card):\n        # Check if card is due based on interval and last reviewed date\n        return True\n\n    def get_today(self):\n        # Return today's date in a suitable format\n        return '2023-10-01' \n\n# Additional methods to handle saving and loading decks would go here.
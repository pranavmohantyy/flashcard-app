import json\nimport os\nimport random\nimport csv\n\nclass Flashcard:\n    def __init__(self, front, back, difficulty):\n        self.front = front\n        self.back = back\n        self.difficulty = difficulty\n        self.interval = 1\n        self.last_reviewed = None\n\nclass Deck:\n    def __init__(self, name):\n        self.name = name\n        self.cards = []\n        self.stats = {\n            'total_cards': 0,\n            'due_today': 0,\n            'accuracy_rate': 0.0\n        }\n\n    def add_card(self, card):\n        self.cards.append(card)\n        self.stats['total_cards'] += 1\n\n    def list_cards(self):\n        return [(card.front, card.back, card.difficulty) for card in self.cards]\n\n    def quiz(self):\n        random.shuffle(self.cards)\n\n    def update_stats(self, correct_answers, total_questions):\n        if total_questions > 0:\n            self.stats['accuracy_rate'] = correct_answers / total_questions\n\n    def due_today_count(self):\n        return self.stats['due_today']\n\n    def reset_due_today(self):\n        self.stats['due_today'] = 0\n